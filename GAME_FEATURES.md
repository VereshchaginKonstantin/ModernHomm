# Игровая функциональность

## Описание

В бот добавлена игровая функциональность с системой управления игровыми профилями пользователей и их юнитами.

## Структура базы данных

### Таблица `game_users` (Игровые пользователи)

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer (PK) | Уникальный идентификатор |
| telegram_id | BigInteger (Unique) | ID пользователя в Telegram |
| name | String(255) | Имя игрока |
| balance | Numeric(12, 2) | Счет игрока (по умолчанию из конфига) |
| wins | Integer | Количество побед (по умолчанию 0) |
| losses | Integer | Количество поражений (по умолчанию 0) |
| created_at | DateTime | Дата создания профиля |
| updated_at | DateTime | Дата последнего обновления |

### Таблица `user_units` (Юниты пользователей)

| Поле | Тип | Описание |
|------|-----|----------|
| id | Integer (PK) | Уникальный идентификатор |
| game_user_id | Integer (FK) | ID игрового пользователя |
| unit_type_id | Integer | ID типа юнита (натуральное число) |
| count | Integer | Количество юнитов данного типа |

**Связи:**
- `game_users` → `user_units` (один ко многим, CASCADE DELETE)

## Конфигурация

В `config.json` добавлена секция `game`:

```json
{
  "game": {
    "initial_balance": 1000
  }
}
```

- `initial_balance` - начальный баланс для новых игроков (по умолчанию 1000)

## Доступные команды бота

### `/play` - Начать игру
Инициализирует игровой профиль пользователя. Если профиль уже существует, показывает текущую статистику.

**Ответ при создании:**
```
🎮 Добро пожаловать в игру, {имя}!

💰 Ваш начальный баланс: ${баланс}
🏆 Побед: 0
💔 Поражений: 0

Удачи в игре!
```

**Ответ при повторном вызове:**
```
👋 С возвращением, {имя}!

💰 Текущий баланс: ${баланс}
🏆 Побед: {побед}
💔 Поражений: {поражений}

Вы уже зарегистрированы в игре!
```

### `/profile` - Просмотр профиля
Показывает игровой профиль с балансом, статистикой и списком юнитов.

**Ответ:**
```
👤 Профиль игрока {имя}

💰 Баланс: ${баланс}
🏆 Побед: {побед}
💔 Поражений: {поражений}

🔰 Ваши юниты:
- Тип юнита #{id}: {количество} шт.
```

### Текстовая команда "Играть"
Пользователь может написать "играть", "play" или "start game" вместо команды `/play`.

## Автоматическая инициализация

При получении **любого первого сообщения** от пользователя автоматически создается игровой профиль с начальным балансом из конфигурации.

## API базы данных (CRUD методы)

### Методы для GameUser

#### `create_game_user(telegram_id, name, initial_balance=1000)`
Создает нового игрового пользователя.

```python
game_user = db.create_game_user(
    telegram_id=12345,
    name="Игрок",
    initial_balance=1500
)
```

#### `get_game_user(telegram_id)`
Получает игрового пользователя по telegram_id.

```python
game_user = db.get_game_user(12345)
```

#### `update_game_user(telegram_id, **kwargs)`
Обновляет данные игрового пользователя.

```python
game_user = db.update_game_user(
    telegram_id=12345,
    balance=2000,
    wins=5,
    losses=2
)
```

#### `delete_game_user(telegram_id)`
Удаляет игрового пользователя (CASCADE удаляет и всех его юнитов).

```python
success = db.delete_game_user(12345)  # True/False
```

#### `get_or_create_game_user(telegram_id, name, initial_balance=1000)`
Получает существующего или создает нового игрового пользователя.

```python
game_user, created = db.get_or_create_game_user(
    telegram_id=12345,
    name="Игрок",
    initial_balance=1000
)
# created = True если пользователь был создан, False если уже существовал
```

### Методы для UserUnit

#### `add_unit(telegram_id, unit_type_id, count=1)`
Добавляет юниты пользователю. Если юнит данного типа уже есть, увеличивает его количество.

```python
unit = db.add_unit(telegram_id=12345, unit_type_id=1, count=10)
```

#### `get_user_units(telegram_id)`
Получает все юниты пользователя.

```python
units = db.get_user_units(12345)
for unit in units:
    print(f"Тип #{unit.unit_type_id}: {unit.count} шт.")
```

#### `update_unit_count(telegram_id, unit_type_id, count)`
Устанавливает новое количество юнитов.

```python
unit = db.update_unit_count(telegram_id=12345, unit_type_id=1, count=20)
```

#### `remove_unit(telegram_id, unit_type_id, count=None)`
Удаляет юниты. Если count=None, удаляет всех юнитов данного типа.

```python
# Удалить 10 юнитов
success = db.remove_unit(telegram_id=12345, unit_type_id=1, count=10)

# Удалить всех юнитов данного типа
success = db.remove_unit(telegram_id=12345, unit_type_id=1)
```

## Примеры использования

### Создание игрового профиля при первом сообщении

```python
# В handle_message
game_user = db.get_game_user(user.id)
if not game_user:
    game_user, created = db.get_or_create_game_user(
        telegram_id=user.id,
        name=user.username or f"User_{user.id}",
        initial_balance=self.initial_balance
    )
```

### Добавление юнитов после покупки

```python
# Добавляем 5 юнитов типа 3
unit = db.add_unit(telegram_id=user.id, unit_type_id=3, count=5)

# Обновляем баланс пользователя
db.update_game_user(telegram_id=user.id, balance=new_balance)
```

### Обработка победы/поражения

```python
# При победе
game_user = db.get_game_user(user.id)
db.update_game_user(
    telegram_id=user.id,
    wins=game_user.wins + 1,
    balance=game_user.balance + reward
)

# При поражении
db.update_game_user(
    telegram_id=user.id,
    losses=game_user.losses + 1
)
```

## Тестирование

Для тестирования CRUD операций запустите:

```bash
python3 test_game_user.py
```

Скрипт проверит все операции создания, чтения, обновления и удаления для игровых пользователей и юнитов.
