#!/bin/bash

# Скрипт для запуска веб-интерфейса ModernHomm

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"

echo "========================================="
echo "Запуск веб-интерфейса ModernHomm"
echo "========================================="

# Проверить, запущен ли postgres
if ! docker ps | grep -q modernhomm_postgres; then
    echo "⚠️  PostgreSQL не запущен. Запускаем..."
    docker compose up -d postgres
    echo "Ожидание готовности PostgreSQL..."
    sleep 5
fi

# Собрать образ веб-интерфейса если нужно
echo "Сборка образа веб-интерфейса..."
docker compose build admin

# Запустить веб-интерфейс
echo "Запуск контейнера веб-интерфейса..."
docker compose up -d admin

# Дождаться запуска
echo "Ожидание запуска веб-интерфейса..."
sleep 3

# Проверить статус
if docker ps | grep -q modernhomm_admin; then
    echo ""
    echo "========================================="
    echo "✅ Веб-интерфейс успешно запущена!"
    echo "========================================="
    echo ""
    echo "🌐 Доступ к веб-интерфейсу:"
    echo "   http://localhost"
    echo ""
    echo "📊 Для просмотра логов:"
    echo "   docker compose logs -f admin"
    echo ""
    echo "🛑 Для остановки:"
    echo "   docker compose stop admin"
    echo ""
    echo "========================================="
else
    echo ""
    echo "❌ Ошибка запуска веб-интерфейса!"
    echo "Просмотрите логи: docker compose logs admin"
    exit 1
fi
